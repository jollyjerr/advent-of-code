fn day17(lines: Vec<String>, is_part_two: bool) -> usize {
    let mut heat_loss = 0;

    

    heat_loss
}

#[cfg(test)]
mod tests {
    use crate::common::read_lines;
    use crate::day17::day17;

    #[test]
    fn test_one() {
        assert_eq!(day17(read_lines( "src/data/day17a.txt" ), false), 102);
    }
}
